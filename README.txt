The code uses external libraries CIMg and Gnuplot. 
The former is just a header file, already included 
in the submision, while the latter needs to be 
installed. Gnuplot uses the Boost library, which may 
also be needed.